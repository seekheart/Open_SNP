install.packages('rCurl, scrapeR')
install.packages('rCurl')
install.packages('RCurl')
install.packages("scrapeR")
install.packages("RCurl")
install.packages("RCurl")
library(RCurl)
install.packages("scrapeR")
library(scrapeR)
install.packages("rvest")
install.packages('sqldf')
install.packages('RSQLite')
setwd("~/github/Open_SNP")
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(sqldf)
library(rjson)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- lapply(json_file, function(x) {as.data.frame(x)})
data <- do.call(rbind,data)
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(sqldf)
library(rjson)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- as.data.frame(do.call(rbind, json_file))
View(data)
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(sqldf)
library(rjson)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json', simplifyDaataFrame = TRUE)
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json', nullValue = NA)
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(j, is.list), NA))
})
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(x, is.list), NA))
})
install.packages('plyr')
library(plyr)
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
library(plyr)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(x, is.list), NA))
})
res <- rbind.fill(data)
View(res)
install.packages("RJSONIO")
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(RJSONIO)
library(plyr)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json', nullValue = NA)
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(x, is.list), NA))
})
res <- rbind.fill(data)
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(RJSONIO)
library(plyr)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json', nullValue = NA)
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(x, is.list), NA))
})
res <- rbind.fill(data)
View(res)
View(res)
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(x, is.list), NA))
})
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
library(plyr)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(x, is.list), NA))
})
data <- rbind.fill(data)
remove.packages("RJSONIO")
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
library(plyr)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(x, is.list), NA))
})
data <- rbind.fill(data)
json_file <- fromJSON(file = 'opensnp.json')
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
library(plyr)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(x, is.list), NA))
})
data <- rbind.fill(data)
#!/usr/bin/Rscript
#Script to make MongoDB from OpenSNP Json
#Mike Tung
#Setup libraries I'll be using
library(rjson)
library(plyr)
#load the json and process it into DF.
json_file <- fromJSON(file = 'opensnp.json')
data <- lapply(json_file, function(x) {
as.data.frame(replace(x, sapply(x, is.list), NA))
})
data <- rbind.fill(data)
View(data)
library(rmongodb)
library(mongolite)
